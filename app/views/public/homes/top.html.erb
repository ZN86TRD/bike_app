<div class="container mt-5">
  <% if user_signed_in? %>
    <div class="row">
      <div class="col-md-8 mx-auto">
        <h2 class="mb-4">タイムライン</h2>
        
        <% if @posts.exists? %>
          <% @posts.each do |post| %>
            <div class="card mb-3 shadow-sm">
              <div class="card-body">
                <div class="d-flex align-items-center mb-2">
                  <strong class="mr-2"><%= link_to post.user.name, user_path(post.user), class: "text-dark" %></strong>
                  <small class="text-muted"><%= post.created_at.strftime('%Y/%m/%d') %></small>
                </div>

                <h5 class="card-title"><%= link_to post.title, post_path(post) %></h5>
                
                <% if post.image.attached? %>
                  <div class="text-center mb-3">
                    <%= image_tag post.image.variant(resize_to_fill: [500, 300]).processed, class: "img-fluid rounded d-block mx-auto" %>
                  </div>
                <% end %>

                <p class="card-text"><%= truncate(post.body, length: 100) %></p>

                <div class="d-flex align-items-center">
                  <%= render "public/favorites/btn", post: post %>
                  <span class="small ml-3 mb-2 text-muted">
                    <i class="far fa-comment"></i> <%= post.post_comments.count %>
                  </span>
                </div>
              </div>
            </div>
          <% end %>
          <div class="text-center mt-4">
            <%= link_to "すべての投稿を見る", posts_path, class: "btn btn-outline-primary" %>
          </div>
        <% else %>
          <div class="text-center py-5 shadow-sm bg-light rounded">
            <p class="text-muted">フォローしている人の投稿がここに表示されます。</p>
            <%= link_to "ユーザーをフォローしてみましょう!", users_path, class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>
    </div>
  <% else %>
    <div class="text-white d-flex align-items-center mb-0" 
        style="background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('<%= asset_path('bike-app_top.jpg') %>') center/cover; min-height: 550px;">
      <div class="container text-center">
        <div class="mb-4">
          <%= image_tag 'bike-app_logo.png', size: '120x120', class: "shadow-sm" %>
        </div>
        <h1 class="display-3 font-weight-bold mb-3">Connect Your Ride</h1>
        <p class="lead mb-5 px-3">サイクリストのためのコミュニティサイト。共に走る仲間と。</p>
        <div class="d-grid gap-3 d-sm-block">
          <%= link_to new_user_registration_path, class: "btn btn-primary btn-lg px-5 shadow-lg mx-2 mb-3" do %>
            今すぐ参加する
          <% end %>
        </div>
      </div>
    </div>
    <div class="py-5">
      <div class="container py-4">
        <div class="text-center mb-5">
          <h2 class="font-weight-bold">さまざまな体験</h2>
        </div>
        <div class="row text-center">
          <div class="col-md-4 mb-5">
            <div class="mb-4 bg-light d-inline-block p-4 rounded-circle text-primary">
              <i class="fas fa-map-marked-alt fa-3x"></i>
            </div>
            <h4 class="font-weight-bold">スポットを検索</h4>
            <p class="text-muted">ルートやお気に入りスポットの共有、全国の絶景スポットやサイクリングコースの情報を手に入れましょう！</p>
          </div>
          <div class="col-md-4 mb-5">
            <div class="mb-4 bg-light d-inline-block p-4 rounded-circle text-success">
              <i class="fas fa-camera fa-3x"></i>
            </div>
            <h4 class="font-weight-bold">思い出の記録</h4>
            <p class="text-muted">愛車の写真を投稿し、共有しましょう。住所とともに保存することでルートマップにも共有可能です！</p>
          </div>
          <div class="col-md-4 mb-5">
            <div class="mb-4 bg-light d-inline-block p-4 rounded-circle text-info">
              <i class="fas fa-users fa-3x"></i>
            </div>
            <h4 class="font-weight-bold">グループ交流</h4>
            <p class="text-muted">掲示板機能付きのグループでユーザー同士で交流。安心の承認制のコミュニティでグループライドに乗り出しましょう！</p>
          </div>
        </div>
      </div>
    </div>
  <% end %>
  <% if user_signed_in? %>
    <div class="d-flex justify-content-center mt-4">
      <%= paginate @posts %>
    </div>
  <% end %>
</div>